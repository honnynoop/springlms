<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lms.exam.mapper.UserMapper">
    <select id="findByUsername" resultType="com.lms.exam.model.User">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.lms.exam.model.User">
        SELECT * FROM users WHERE user_id = #{userId}
    </select>

    <select id="findAll" resultType="com.lms.exam.model.User">
        SELECT * FROM users ORDER BY created_at DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (username, password, email, full_name, phone, role, is_active)
        VALUES (#{username}, #{password}, #{email}, #{fullName}, #{phone}, #{role}, #{isActive})
    </insert>

    <update id="update">
        UPDATE users
        SET username = #{username},
            password = #{password},
            email = #{email},
            full_name = #{fullName},
            phone = #{phone},
            role = #{role},
            is_active = #{isActive}
        WHERE user_id = #{userId}
    </update>

    <delete id="delete">
        DELETE FROM users WHERE user_id = #{userId}
    </delete>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM users
    </select>
</mapper>
